import React from "react";
import { AppBar, Typography } from "@material-ui/core";

export default function Navbar() {
  return (
    <div>
      <AppBar position="fixed">
        <Typography variant="h3">helloffffff</Typography>
      </AppBar>
    </div>
  );
}
